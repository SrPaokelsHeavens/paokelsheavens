---
import '../styles/global.css';
import NotificationPrompt from '../components/ui/NotificationPrompt.astro';

interface Props {
	title: string;
	description?: string;
}

const { title, description = "Sr.Paokel's Sect Scriptorium. Chronicles of Immortals." } = Astro.props;
const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
	if (path === '/') return currentPath === '/';
	return currentPath === path || currentPath.startsWith(`${path}/`);
};
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title} | PAOKELSHEAVENS</title>
		
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
		<style is:global>
			/* Force Header Visibility - Ultimate Fortification */
			#global-header {
				display: flex !important;
				visibility: visible !important;
				opacity: 1 !important;
				transform: none !important;
			}
			
			/* Force Desktop Elements on Large Screens */
			@media (min-width: 768px) {
				#global-header ul {
					display: flex !important;
				}
				#global-header .sm-inline-logo {
					display: inline !important;
				}
				#global-header .sm-hidden-logo {
					display: none !important;
				}
				#sect-seal-btn {
					display: none !important;
				}
			}

			/* Mobile Styles */
			@media (max-width: 767px) {
				#global-header ul {
					display: none !important;
				}
				#global-header .sm-inline-logo {
					display: none !important;
				}
				#global-header .sm-hidden-logo {
					display: inline !important;
				}
				#sect-seal-btn {
					display: flex !important;
				}
			}
		</style>
	</head>
	<body class="min-h-screen flex flex-col bg-void text-[#D1D1C7]">
		<header id="global-header" class="fixed top-0 left-0 w-full z-[100] border-b border-gold/10 bg-void/80 backdrop-blur-md">
			<nav class="container mx-auto px-4 h-16 flex items-center justify-between">
				<a href="/" class="text-xl font-serif font-bold text-gold tracking-tighter hover:scale-105 transition-transform">
					<span class="sm-inline-logo hidden sm:inline">PAOKELSHEAVENS</span>
					<span class="sm-hidden-logo inline sm:hidden">PKH</span>
				</a>
				
				<ul class="nav-links-desktop hidden md:flex gap-8 font-serif text-[10px] tracking-widest overflow-x-auto no-scrollbar py-2">
					<li>
						<a href="/library" class={`transition-all duration-300 whitespace-nowrap pb-1 ${isActive('/library') ? 'text-gold border-b border-gold shadow-[0_5px_15px_-2px_rgba(197,160,89,0.3)]' : 'text-[#D1D1C7]/40 hover:text-gold'}`}>
							LIBRARY
						</a>
					</li>
					<li>
						<a href="/news" class={`transition-all duration-300 whitespace-nowrap pb-1 ${isActive('/news') ? 'text-gold border-b border-gold shadow-[0_5px_15px_-2px_rgba(197,160,89,0.3)]' : 'text-[#D1D1C7]/40 hover:text-gold'}`}>
							NEWS
						</a>
					</li>
					<li>
						<a href="/dao-table" class={`transition-all duration-300 whitespace-nowrap pb-1 ${isActive('/dao-table') ? 'text-gold border-b border-gold shadow-[0_5px_15px_-2px_rgba(197,160,89,0.3)]' : 'text-[#D1D1C7]/40 hover:text-gold'}`}>
							DAO TABLE
						</a>
					</li>
					<li>
						<a href="/community" class={`transition-all duration-300 whitespace-nowrap pb-1 ${isActive('/community') ? 'text-gold border-b border-gold shadow-[0_5px_15px_-2px_rgba(197,160,89,0.3)]' : 'text-[#D1D1C7]/40 hover:text-gold'}`}>
							COMMUNITY
						</a>
					</li>
				</ul>
				
				<div class="flex items-center gap-4">
					<a href="/donations" class="inline-block bg-gold/10 border border-gold/30 px-4 py-2 rounded text-[9px] font-serif text-gold hover:bg-gold hover:text-void transition-all uppercase tracking-[0.2em]">
						DONATE
					</a>
				</div>
			</nav>
		</header>

		<!-- Mobile Navigation - The Sect Seal -->
		<div id="mobile-menu-overlay" class="fixed inset-0 bg-void/95 z-[100] flex flex-col items-center justify-center opacity-0 pointer-events-none transition-all duration-500 backdrop-blur-lg">
			<button id="close-menu" class="absolute top-8 right-8 text-gold/40 hover:text-gold transition-colors text-2xl font-light">×</button>
			<nav class="flex flex-col gap-10 text-center">
				<a href="/library" class="mobile-link text-2xl font-serif text-gold tracking-[0.3em] uppercase opacity-0 translate-y-4">LIBRARY</a>
				<a href="/news" class="mobile-link text-2xl font-serif text-gold tracking-[0.3em] uppercase opacity-0 translate-y-4" style="transition-delay: 100ms;">NEWS</a>
				<a href="/dao-table" class="mobile-link text-2xl font-serif text-gold tracking-[0.3em] uppercase opacity-0 translate-y-4" style="transition-delay: 200ms;">DAO TABLE</a>
				<a href="/community" class="mobile-link text-2xl font-serif text-gold tracking-[0.3em] uppercase opacity-0 translate-y-4" style="transition-delay: 300ms;">COMMUNITY</a>
				<a href="/donations" class="mobile-link text-xs font-serif text-gold/40 tracking-[0.5em] uppercase border border-gold/20 px-8 py-3 opacity-0 translate-y-4" style="transition-delay: 400ms;">DONATE</a>
			</nav>
		</div>

		<button id="sect-seal-btn" class="md:hidden fixed bottom-8 right-8 z-[110] w-14 h-14 bg-void border border-gold/40 rounded-full flex items-center justify-center shadow-[0_0_20px_rgba(197,160,89,0.2)] hover:shadow-[0_0_30px_rgba(197,160,89,0.4)] transition-all group overflow-hidden">
			<div class="absolute inset-0 bg-gold/5 group-hover:bg-gold/10 transition-colors"></div>
			<div class="relative flex flex-col gap-1.5 items-center">
				<span class="w-5 h-px bg-gold transition-all duration-300"></span>
				<span class="w-3 h-px bg-gold transition-all duration-300"></span>
				<span class="w-5 h-px bg-gold transition-all duration-300"></span>
			</div>
		</button>

		<script is:inline>
			// Global Silence Seal - Ensures no audio or immersive state leaks between pages
			(function() {
				const silence = () => {
					if ('speechSynthesis' in window) {
						window.speechSynthesis.cancel();
					}
				};

				// Kill any leftover speech on load
				silence();
				
				// Handle Astro transitions
				document.addEventListener('astro:after-swap', silence);
				document.addEventListener('astro:before-preparation', silence);
			})();

			// Header Sentinel - Protects the navigation from being deleted or hidden
			(function() {
				const protect = () => {
					const header = document.getElementById('global-header');
					if (header) {
						header.style.setProperty('display', 'flex', 'important');
						header.style.setProperty('visibility', 'visible', 'important');
						header.style.setProperty('opacity', '1', 'important');
					}
				};
				
				// Run immediately and on intervals to fight aggressive scripts
				protect();
				setInterval(protect, 1000);
				
				// Prevent deletion
				const observer = new MutationObserver((mutations) => {
					mutations.forEach((mutation) => {
						mutation.removedNodes.forEach((node) => {
							if (node.id === 'global-header') {
								console.warn('Sect Warning: An external force tried to delete the Heavenly Header. Restoring...');
								document.body.prepend(node);
							}
						});
					});
				});
				observer.observe(document.body, { childList: true });
			})();

			document.addEventListener('DOMContentLoaded', () => {
				const sealBtn = document.getElementById('sect-seal-btn');
				const overlay = document.getElementById('mobile-menu-overlay');
				const closeBtn = document.getElementById('close-menu');
				const mobileLinks = document.querySelectorAll('.mobile-link');

				if (!sealBtn || !overlay || !closeBtn) return;

				const toggleMenu = (show) => {
					if (show) {
						overlay.style.display = 'flex';
						setTimeout(() => {
							overlay.classList.remove('opacity-0', 'pointer-events-none');
							sealBtn.style.opacity = '0';
							sealBtn.style.pointerEvents = 'none';
						}, 10);
						mobileLinks.forEach((link, i) => {
							setTimeout(() => {
								link.classList.remove('opacity-0', 'translate-y-4');
								link.classList.add('opacity-100', 'translate-y-0');
							}, 100 + (i * 50));
						});
					} else {
						overlay.classList.add('opacity-0', 'pointer-events-none');
						sealBtn.style.opacity = '1';
						sealBtn.style.pointerEvents = 'auto';
						mobileLinks.forEach(link => {
							link.classList.add('opacity-0', 'translate-y-4');
							link.classList.remove('opacity-100', 'translate-y-0');
						});
						setTimeout(() => {
							overlay.style.display = 'none';
						}, 500);
					}
				}

				sealBtn.addEventListener('click', () => toggleMenu(true));
				closeBtn.addEventListener('click', () => toggleMenu(false));
				
				mobileLinks.forEach(link => {
					link.addEventListener('click', () => toggleMenu(false));
				});

				// Smart Donation Mechanism
				document.addEventListener('click', (e) => {
					const target = e.target.closest('.smart-donate');
					if (target && target.tagName === 'A') {
						e.preventDefault();
						const url = target.href;
						const isAndroid = /Android/i.test(navigator.userAgent);
						const isIOS = /iPhone|iPad|iPod/i.test(navigator.userAgent);

						if (isAndroid) {
							window.location.href = `intent://${url.replace(/^https?:\/\//, '')}#Intent;scheme=https;end`;
						} else if (isIOS) {
							window.open(url, '_blank');
						} else {
							window.location.href = url;
						}
					}
				});
			});
		</script>

		<main class="flex-grow pt-16">
			<slot />
		</main>

		<footer class="border-t border-gold/10 bg-void-light py-12">
			<div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-3 gap-8 text-center md:text-left">
				<div>
					<h3 class="text-gold font-serif mb-4 uppercase tracking-widest">PAOKELSHEAVENS</h3>
					<p class="text-[11px] text-[#D1D1C7]/60 leading-relaxed italic">
						Original cultivation chronicles and sect legends. Ascension is a lonely path, but here we share the annals.
					</p>
				</div>
				<div>
					<h4 class="text-gold font-serif mb-4 text-[9px] tracking-widest uppercase opacity-40">Navigation</h4>
					<ul class="text-[10px] space-y-2 text-[#D1D1C7]/60">
						<li><a href="/library" class="hover:text-gold transition-colors">Library</a></li>
						<li><a href="/dao-table" class="hover:text-gold transition-colors">Dao Table</a></li>
					</ul>
				</div>
				<div>
					<h4 class="text-gold font-serif mb-4 text-[9px] tracking-widest uppercase opacity-40">Signals</h4>
					<div class="flex justify-center md:justify-start gap-4 text-[10px]">
						<a href="/community" class="text-[#D1D1C7]/60 hover:text-gold transition-colors">Community Hub</a>
						<a href="/news" class="text-[#D1D1C7]/60 hover:text-gold transition-colors">Newsroom</a>
						<a href="mailto:admin@srpaokelsheavens.eu.org" class="text-[#D1D1C7]/60 hover:text-gold transition-colors">Email</a>
					</div>
				</div>
			</div>
			<div class="mt-12 text-center text-[9px] text-gold/20 uppercase tracking-[0.3em]">
				© 2026 PAOKELSHEAVENS - Immortal Records
			</div>
		</footer>

		<NotificationPrompt />
	</body>
</html>

